nodes:
  - node_id: +python#2
    title: "Download Image"
    icon: ":carbon:download:"
    task:
      ui:
        default_width: 450
      inputs_def:
        - handle: item
          description: "Image URL to download from the web"
          json_schema:
            type: string
        - handle: save_dir
          description: "Target directory path where the downloaded image will be saved"
          json_schema:
            type: string
            ui:widget: dir
          nullable: true
      outputs_def:
        - handle: item
          description: "Local file path of the successfully downloaded image"
          json_schema:
            type: string
          nullable: false
      executor:
        name: python
        options:
          entry: scriptlets/+scriptlet#1.py
      title: "Download Image"
      description: "Downloads an image from a URL and saves it to the specified
        directory. If no directory is specified, saves to the session
        directory."
    inputs_from:
      - handle: item
        from_flow:
          - input_handle: item
      - handle: save_dir
        value:
outputs_from:
  - handle: item
    from_node:
      - node_id: +python#2
        output_handle: item
